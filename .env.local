# Leave empty for local development (uses Vite proxy to http://localhost:5000)
# Or set to http://localhost:5000 to hit the local backend directly.
# For production, set this in Vercel environment variables.
VITE_API_URL=
