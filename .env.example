# Backend origin (bare, no trailing /api). axios appends /api automatically.
# Local dev: leave unset to use the Vite dev proxy, or set it to hit a backend directly.
# Production (Vercel): set this in Project Settings → Environment Variables.
VITE_API_URL=https://citywala-backend-production.up.railway.app
